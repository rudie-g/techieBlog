<h1 class="homepage-header">Welcome to techieBlog</h1>
    
    {{#each postData as |newpost|}}
    <div class="post-container">
        <h4>{{title}}</h4>
        <p>{{date}}</p>
        <a href="/post/{{id}}">View</a>
    </div>
    {{/each}}